# Контракт проекта

Единственный файл скилла, который заполняется под конкретный проект. Справочники в `references/` собственных путей и имён не содержат — вместо них стоят placeholder'ы `<слот>` из таблиц ниже.

Пока слот пуст (`—`), скилл не имеет права угадывать значение: сначала бутстрап по `SKILL.md`, потом работа.

Проект: `—`
Платформа: 1С-Битрикс
Сборка: `—`

## Пути

| Слот | Что это | Значение |
|---|---|---|
| `<template-root>` | корень шаблона сайта | `—` |
| `<scss-entry>` | точка входа SCSS | `—` |
| `<blocks-dir>` | папка файлов BEM-блоков | `—` |
| `<blocks-index>` | индекс блоков и способ подключения | `—` |
| `<pages-dir>` | стили одной страницы | `—` |
| `<ui-dir>` | UI-примитивы | `—` |
| `<helpers-dir>` | миксины и хелперы | `—` |
| `<js-entry>` | точка входа общесайтового JS | `—` |
| `<ext-dir>` | каталог расширений | `—` |

Что нельзя редактировать: `—` (ядро платформы плюс генерируемые артефакты сборки).

## SCSS API

| Слот | Что это | Значение |
|---|---|---|
| `<helpers-import>` | первая строка файла блока | `—` |
| `<media-xs>` `<media-sm>` `<media-md>` `<media-lg>` | миксины адаптива от меньшего к большему | `—` |
| `<hover-mixin>` | миксин hover-состояния | `—` |
| `<transition-mixin>` | миксин transition | `—` |
| `<typography-mixin>` | миксины типографики | `—` |
| `<typography-class>` | те же стили классами в HTML | `—` |
| `<container>` | класс ограничения ширины | `—` |
| `<page-wrapper>` | корневой блок каркаса и его зоны | `—` |

## Токены и темы

| Слот | Что это | Значение |
|---|---|---|
| `<tokens-map>` | человекочитаемая карта токенов | `—` |
| `<tokens-impl>` | файл, где токены объявлены | `—` |
| `<token-text>` `<token-muted>` | цвет основного и приглушённого текста | `—` |
| `<token-bg>` `<token-surface>` | фон страницы и поверхностей | `—` |
| `<token-border>` `<token-accent>` | границы и акцент | `—` |
| `<token-font>` | шрифтовые токены | `—` |
| `<themes>` | темы и способ переключения | `—` |
| `<design-source>` | исходный макет | `—` |

Если тем больше одной, цвет, взятый raw-значением вместо семантического токена, ломает блок в одной из тем.

## JS

| Слот | Что это | Значение |
|---|---|---|
| `<ns>` | неймспейс расширений | `—` |
| `<APP>` | глобальный объект проекта | `—` |
| `<onReady>` | диспетчер готовности DOM | `—` |
| `<initSlider>` | инициализатор слайдеров и форма его конфига | `—` |

Действующие расширения:

| Расширение | Что даёт | Зависит от |
|---|---|---|
| `—` | | |

Грузится глобально из `header.php`: `—`

Эталонные компоненты для правил `references/bitrix-assets.md`: порядок CSS — `—`, условная загрузка — `—`.

## Что описано вне скилла

Правила согласования правок, карта проекта и сборка: `—` (обычно `CLAUDE.md` / `AGENTS.md` в корне).

Проектные архитектурные документы, которые скилл не дублирует: `—`
